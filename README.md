# -Digital-Image
lesson of digital Image code and lesson

To run it, on your current setup:

pip install mediapipe opencv-python
python "holistic_landmarks_tasks_api.py"
pip install opencv-contrib-python
pip install numpy
pip install opencv-python
pip install matplotlib
pip install requests
